#include <Liquium.hpp>
#include <Windows.h>

int WinMain(HINSTANCE instance, HINSTANCE previnstance, PSTR lpcommand_line, int ncmd_show)
{
    //liq::string str("Hello there");
    //return str.length;
    return 0;
}